<div class="row">
  <div class="col-md-6">
    <div class="page-header">
      <h1>Purchase a new phone number</h1>
    </div>

    <p>
      The number you choose will be used to create a Lead Source. On the next page, you will set a name and forwarding number for this lead source.
    </p>

    <table class="table table-striped">
      <thead>
        <tr>
          <th>Phone number</th>
          <th>City</th>
          <th>State</th>
          <th>Zip</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
      <% @phone_numbers.each do |number| %>
        <tr>
          <td><%= number.friendly_name %></td>
          <td><%= number.rate_center %></td>
          <td><%= number.region %></td>
          <td><%= number.postal_code %></td>
          <td>
            <%= link_to 'Purchase', lead_sources_path(number.phone_number), method: :post, class: 'btn btn-primary btn-xs' %>
          </td>
        </tr>
      <% end %>
      </tbody>
    </table>
  </div>
</div>
